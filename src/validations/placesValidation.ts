import Joi from "joi";


export const addPlaceValidation = Joi.object({

});

export const  getPlacesValidation = Joi.object({

});

export const getPlaceByIdValidation = Joi.object({

});

export const updatePlaceValidation = Joi.object({

});

export const deletePlaceValidation = Joi.object({

});